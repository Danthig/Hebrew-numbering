# tkinter בדרך כלל מגיע עם Python, אבל אם לא:
# Windows: pip install python-tkinter
# Linux (Ubuntu/Debian): sudo apt-get install python3-tk
# macOS: brew install python-tk

# הליבה, ה-CLI וממשק ה-tkinter — ללא תלויות חיצוניות.
#
# הממשק הגרפי המודרני (HTML/CSS) דורש pywebview (אופציונלי):
#   pip install pywebview
# אם pywebview לא מותקן, המערכת נופלת אוטומטית לממשק tkinter.
